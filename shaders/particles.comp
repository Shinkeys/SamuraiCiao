#version 460 core


layout (local_size_x = 1, local_size_y = 1,  local_size_z = 1) in;

layout(std430, binding = 1) buffer vertices
{
    vec4 positions[];
};


void main()
{
    uint idx = gl_GlobalInvocationID.x;
    
    positions[idx].xyz += vec3(0.1);
}